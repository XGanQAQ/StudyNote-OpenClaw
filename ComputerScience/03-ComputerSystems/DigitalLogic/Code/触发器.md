
- **只在时钟的某一个瞬间采样输入**    
- 其他时间输入怎么变都不影响输出

触发器赋予电路“记忆”功能，其输出保持为最近输入口的特征

## R-S 触发器

Reset-Set 复位 - 置位 触发器

## D型锁存器/触发器
锁定一位数据

## D型触发器

### 边沿触发的D型触发器
CLK输入口的信号发生的跳变为正跳变/正边沿（从0到1的跳变）的瞬间锁定数据

### 带预置和清零的边沿触发的D型触发器

## 波纹计数器