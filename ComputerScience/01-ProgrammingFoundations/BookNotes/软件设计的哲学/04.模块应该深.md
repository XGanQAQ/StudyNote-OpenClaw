模块的接口将复杂性隐藏起来。
用户只需要知道接口提供的抽象即可。

**模块应该深**
深模块相比浅模块，他提供更少的接口，但是接口背后封装了大量的细节。虽然说背后的代码较为复杂，但是这样也减少了用户理解接口的负担。
浅模块虽然提供了更多的接口，但是接口背后的实现并没有很复杂。反而大量的接口的负担抵消了其隐藏的实现的便利。

有时候必要的时候，也会实现浅模块，但是浅模块对于降低复杂性并没有太大帮助。