**直通方法** 
指的是只将字段的方法直接传递给，其他什么也不做。其实就是浅方法。说明类之间的职责划分出了问题

**装饰器模式** 
装饰器模式在原本基础的方法上，对其进行包装，添加额外的功能，但一般这些功能都比较浅。 

**是否应该使用装饰器模式？**
- 能否直接添加到底层 
- 能发划分为专门针对场景的类
- 能否合并装饰器
- 是否需要封装

**不同层 不同抽象背后思想** 
任何额外的系统设计函数 接口 字段 抽象 都会增加系统的复杂性。使用同一抽象，就意味着封装背后带来的收益少。