### 答案思路（简洁版，面试直接用）：

- **原因**：string 在 C# 中被设计成不可变（immutable），是为了 **安全性**（避免引用被修改）、**性能优化**（字符串驻留 / Interning）、**多线程安全**。
    
- **影响**：每次修改字符串都会生成新对象，所以频繁拼接时推荐用 `StringBuilder`。
    

---

### 面试可说的答案（简短一句话版）：

👉 _“因为 string 是不可变类型，这样可以保证多线程安全、支持字符串驻留优化，同时避免修改带来的不可控副作用。”_
