# 堆 (Heap)

## 定义
堆是一种**完全二叉树**，分为最大堆和最小堆。

- **最大堆**：父节点 ≥ 子节点
- **最小堆**：父节点 ≤ 子节点

## 操作复杂度
| 操作 | 复杂度 |
|------|--------|
| 插入 | O(log n) |
| 删除堆顶 | O(log n) |
| 建堆 | O(n) |
| 获取最值 | O(1) |

## 应用
- **优先队列**：进程调度、任务管理
- **堆排序**：基于最大堆/最小堆的排序
- **Top K 问题**：维护大小为 K 的最小堆
