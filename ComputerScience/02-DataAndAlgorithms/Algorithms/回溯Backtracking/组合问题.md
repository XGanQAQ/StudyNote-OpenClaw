[第77题. 组合 | 代码随想录](https://programmercarl.com/0077.%E7%BB%84%E5%90%88.html#%E7%AE%97%E6%B3%95%E5%85%AC%E5%BC%80%E8%AF%BE)
[77. 组合 - 力扣（LeetCode）](https://leetcode.cn/problems/combinations/description/)

``` cs
    public IList<IList<int>> Combine(int n, int k)
    {
        IList<IList<int>> res = new List<IList<int>>();

        void BackTrack(int start, List<int> path)
        {
            if (path.Count == k)
            {
                res.Add(new List<int>(path));
            }
            else
            {
                for (int i = start; i <= n; i++)
                {
                    path.Add(i);
                    BackTrack(i + 1, path);
                    path.RemoveAt(path.Count - 1);
                }
            }
        }
        
        BackTrack(1,new List<int>());
        return res;
    }
```

回溯的本质同样是暴力破解

利用递归来 实现不同深度的遍历循环（因为组合数量k不同）

通过递归来构造path，类似一种深度搜索