- 单例管理器
	- 我希望单例不再依赖Unity的生命周期进行初始化
	- 我还希望能有个编辑器界面的管理器，可以看到当前都有哪些单例可以获取
	- 不依赖MonoBehaviour进行初始化的单例管理器，没办法利用编辑界面进行序列化的配置

为什么需要一个单例管理器-Unity编辑器

编辑器的本质是为了方便，如果有一个单例管理器编辑器，我可以很方便的查看到各个单例的状态是什么样的

正常来说单例分为3中
1. 纯c#单例
2. 跨场景Mono单例
3. 单场景单例

场景单例正常来说是直接在Hierarchy中可以查看到

## 问题
1. 即使用编辑器，我任然无法查看字典之类的类型（除非自己去解决，去拓展）
2. 场景单例可以直接在Hierarchy中查看
3. 纯C#单例也可以通过Debug模式查看

## 结论
暂时没必要实现